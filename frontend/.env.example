# Base URL of the backend API (see /backend/.env.example for the server port).
# In production this is same-origin (https://greenbarbetadventures.com/api/v1) —
# the reverse proxy forwards /api/ to the backend, so no CORS preflight applies.
VITE_API_BASE_URL=http://localhost:3070/api/v1
